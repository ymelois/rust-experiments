[build]
target = "x86_64-unknown-none"

[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-args=-nostartfiles"]

[profile.dev]
lto = "fat"
panic = "abort"

[profile.release]
opt-level = 3
debug = "none"
strip = "symbols"
lto = "fat"
panic = "abort"
codegen-units = 1
